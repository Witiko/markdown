% Load the package.
\input markdown

% Load the support files.
\markdownSetup {
  eagerCache = false,
  outputDir = OUTPUT_DIRECTORY,
  import = {
    witiko/markdown/test = snippet as testSnippet,
  }
}
